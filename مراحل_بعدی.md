# 🚀 مراحل بعدی - راهنمای عملی

## 📋 وضعیت فعلی
✅ سیستم آماده است  
✅ تست‌ها انجام شده  
✅ مستندات ایجاد شده  
⏳ آماده برای استفاده

---

## 🎯 گام 1: راه‌اندازی سرور

### در ترمینال PowerShell:

```powershell
# رفتن به دایرکتوری پروژه
cd N:\nima-ai-marketing

# راه‌اندازی سرور
python run_api.py
```

**یا با uvicorn مستقیم:**
```powershell
uvicorn api.main:app --host 127.0.0.1 --port 8000 --reload
```

**انتظار:** باید پیام زیر را ببینید:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

---

## 🧪 گام 2: تست سرور

### در یک ترمینال جدید (PowerShell دیگر):

```powershell
cd N:\nima-ai-marketing

# تست سلامت سرور
python test_backend_health.py

# یا تست ساده
python test_system_check.py
```

**انتظار:** باید `✅ سرور در حال اجرا است` را ببینید

---

## 🔍 گام 3: تست Endpoint اصلی

### تست `/analyze-url`:

```powershell
# تست با URL ساده
python test_analyze_url.py https://example.com

# یا تست با URL خاص خودتان
python test_analyze_url.py https://your-website.com
```

**انتظار:** باید نتایج تحلیل را ببینید شامل:
- Visual Trust analysis
- Brain scores (Friction, Trust, Clarity)
- Features extraction
- Recommendations

---

## 🌐 گام 4: استفاده از Frontend (اختیاری)

### راه‌اندازی Frontend:

```powershell
# در یک ترمینال جدید
cd N:\nima-ai-marketing\web
python -m http.server 8080
```

**سپس در مرورگر:**
- باز کنید: `http://localhost:8080`
- صفحه اصلی را می‌بینید
- می‌توانید URL را وارد کنید و تحلیل کنید

---

## 📝 گام 5: استفاده از API در کد خودتان

### مثال Python:

```python
import requests

# تحلیل یک URL
response = requests.post(
    "http://127.0.0.1:8000/analyze-url",
    json={"url": "https://example.com"},
    timeout=60
)

if response.status_code == 200:
    result = response.json()
    print(f"Trust Score: {result['brain']['trustScore']}")
    print(f"Friction Score: {result['brain']['frictionScore']}")
    print(f"Decision Probability: {result['brain']['decisionProbability']}")
else:
    print(f"Error: {response.status_code}")
    print(response.text)
```

### مثال JavaScript (برای Frontend):

```javascript
async function analyzeURL(url) {
  const response = await fetch('http://127.0.0.1:8000/analyze-url', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url: url })
  });
  
  const data = await response.json();
  return data;
}

// استفاده
analyzeURL('https://example.com')
  .then(result => {
    console.log('Trust Score:', result.brain.trustScore);
    console.log('Friction Score:', result.brain.frictionScore);
  });
```

---

## 🔧 گام 6: عیب‌یابی (اگر مشکلی پیش آمد)

### مشکل: سرور راه‌اندازی نمی‌شود

**بررسی:**
```powershell
# بررسی Python
python --version

# بررسی dependencies
pip install -r requirements.txt

# بررسی port
netstat -ano | findstr :8000
```

### مشکل: خطای "Method Not Allowed"

**راه حل:**
- ✅ از `POST` استفاده کنید (نه GET)
- ✅ path صحیح: `/analyze-url` (بدون `/api/`)
- ✅ body باید JSON باشد: `{"url": "..."}`

**تست:**
```powershell
python test_method_error.py
```

### مشکل: خطای Connection

**راه حل:**
- بررسی کنید سرور در حال اجرا است
- بررسی کنید port 8000 آزاد است
- بررسی کنید firewall مسدود نکرده

---

## 📊 گام 7: بررسی نتایج

### فایل‌های ایجاد شده:

1. **Cache:** `api/cache/` - نتایج cache شده (30 دقیقه TTL)
2. **Debug Screenshots:** `api/debug_shots/` - Screenshot های ذخیره شده
3. **Logs:** در console سرور

### بررسی Cache:

```python
import json
from pathlib import Path

cache_dir = Path("api/cache")
for cache_file in cache_dir.glob("*.json"):
    data = json.loads(cache_file.read_text())
    print(f"URL: {data.get('url')}")
    print(f"Trust Score: {data['brain']['trustScore']}")
```

---

## 🎨 گام 8: استفاده پیشرفته

### 1. Bypass Cache:

```python
response = requests.post(
    "http://127.0.0.1:8000/analyze-url",
    json={
        "url": "https://example.com",
        "refresh": True  # bypass cache
    }
)
```

### 2. استفاده از Query Parameter:

```python
response = requests.post(
    "http://127.0.0.1:8000/analyze-url?refresh=true",
    json={"url": "https://example.com"}
)
```

### 3. تحلیل چند URL:

```python
urls = [
    "https://example.com",
    "https://google.com",
    "https://github.com"
]

results = []
for url in urls:
    response = requests.post(
        "http://127.0.0.1:8000/analyze-url",
        json={"url": url},
        timeout=60
    )
    if response.status_code == 200:
        results.append(response.json())

# مقایسه نتایج
for r in results:
    print(f"{r['url']}: Trust={r['brain']['trustScore']}")
```

---

## 📚 مستندات موجود

### فایل‌های راهنما:
1. `گزارش_کامل_سیستم.md` - گزارش کامل سیستم
2. `راهنمای_استفاده_Endpoint.md` - راهنمای استفاده از API
3. `test_method_error.py` - عیب‌یابی خطاها
4. `test_analyze_url.py` - تست endpoint

### فایل‌های تست:
- `test_system_check.py` - بررسی وضعیت سیستم
- `test_backend_health.py` - تست سلامت
- `test_analyze_url.py` - تست URL analysis
- `test_method_error.py` - عیب‌یابی

---

## ✅ چک‌لیست سریع

- [ ] سرور راه‌اندازی شده (`python run_api.py`)
- [ ] تست سلامت موفق (`python test_backend_health.py`)
- [ ] تست endpoint موفق (`python test_analyze_url.py`)
- [ ] Frontend راه‌اندازی شده (اختیاری)
- [ ] API در کد خودتان کار می‌کند

---

## 🎯 خلاصه - چه کار کنید:

### الان (فوری):
1. ✅ سرور را راه‌اندازی کنید: `python run_api.py`
2. ✅ در ترمینال دیگر تست کنید: `python test_analyze_url.py https://example.com`

### بعداً (برای استفاده):
1. ✅ از API در کد خودتان استفاده کنید
2. ✅ یا از Frontend استفاده کنید
3. ✅ نتایج را بررسی و استفاده کنید

---

## 💡 نکات مهم

1. **سرور باید همیشه در حال اجرا باشد** برای استفاده از API
2. **Cache:** نتایج برای 30 دقیقه cache می‌شوند (برای سرعت)
3. **Timeout:** تحلیل ممکن است 30-60 ثانیه طول بکشد
4. **Screenshots:** در `api/debug_shots/` ذخیره می‌شوند
5. **Logs:** خطاها در console سرور نمایش داده می‌شوند

---

## 🆘 اگر سوالی دارید:

1. بررسی کنید `راهنمای_استفاده_Endpoint.md`
2. اجرا کنید `python test_method_error.py`
3. بررسی کنید لاگ‌های سرور
4. مستندات را بخوانید

---

**آماده استفاده! 🚀**













