# اسکریپت‌های مدیریت سرور

این پوشه شامل اسکریپت‌های PowerShell برای مدیریت و تست سرور FastAPI است.

## 🚀 راه‌اندازی سرور

### روش استاندارد (توصیه می‌شود)

```powershell
# 1. متوقف کردن تمام پروسه‌های قبلی روی پورت 8000
.\scripts\kill_port_8000.ps1

# 2. راه‌اندازی سرور
.\scripts\run_api.ps1
```

این روش تضمین می‌کند که:
- ✅ هیچ پروسه قبلی روی پورت 8000 باقی نمی‌ماند
- ✅ فقط یک سرور روی پورت 8000 اجرا می‌شود
- ✅ محیط مجازی به درستی فعال می‌شود

## 🧪 تست API

### تست endpoint `/api/analyze/url-human`

```powershell
.\scripts\test_url_human.ps1
```

این اسکریپت:
- ✅ از روش استاندارد PowerShell استفاده می‌کند
- ✅ JSON را به درستی ارسال می‌کند
- ✅ خطاها را به صورت واضح نمایش می‌دهد

## 📋 فایل‌های موجود

### `kill_port_8000.ps1`
متوقف کردن تمام پروسه‌های در حال اجرا روی پورت 8000

### `run_api.ps1`
راه‌اندازی سرور FastAPI با محیط مجازی

### `test_url_human.ps1`
تست استاندارد endpoint `/api/analyze/url-human`

## ⚙️ تنظیمات

### متغیرهای محیطی

سرور به صورت خودکار فایل `.env` را از ریشه پروژه لود می‌کند:

```
OPENAI_API_KEY=sk-proj-YOUR_KEY_HERE
OPENAI_MODEL=gpt-4o-mini
```

### بررسی تنظیمات

```powershell
# بررسی وضعیت ENV
Invoke-WebRequest -Uri "http://127.0.0.1:8000/debug/env" | ConvertFrom-Json
```

## 🔧 عیب‌یابی

### مشکل: پورت 8000 در حال استفاده است

```powershell
# راه‌حل: متوقف کردن تمام پروسه‌ها
.\scripts\kill_port_8000.ps1
```

### مشکل: خطای JSON decode

- ✅ از `ConvertTo-Json` در PowerShell استفاده کنید
- ✅ از `Invoke-RestMethod` به جای `curl` استفاده کنید
- ✅ مطمئن شوید که `ContentType` روی `application/json` تنظیم شده است

### مشکل: خطای LLM_UNAVAILABLE

- ✅ بررسی کنید که `OPENAI_API_KEY` در `.env` تنظیم شده است
- ✅ بررسی کنید که API key معتبر است
- ✅ بررسی کنید که اینترنت متصل است

## 📝 نکات مهم

1. **همیشه از اسکریپت‌ها استفاده کنید**: این اسکریپت‌ها تضمین می‌کنند که همه چیز به درستی کار می‌کند

2. **قبل از راه‌اندازی، پورت را پاک کنید**: همیشه `kill_port_8000.ps1` را قبل از `run_api.ps1` اجرا کنید

3. **از تست استاندارد استفاده کنید**: `test_url_human.ps1` روش صحیح تست API را نشان می‌دهد




